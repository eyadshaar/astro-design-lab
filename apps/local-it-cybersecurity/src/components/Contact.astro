---
const phoneNumber = "(503) 555-0142";
const phoneHref = "tel:+150****0142";
const address = "812 SW Washington St, Suite 1200, Portland, OR 97205";
---

<section class="contact section" id="contact">
  <div class="container">
    <div class="contact-layout">
      <!-- Left: Info -->
      <div class="contact-info">
        <div class="section-header reveal" style="text-align:left">
          <span class="tag">Contact Us</span>
          <h2><span class="gradient-text">Let's Talk IT</span></h2>
          <p>Whether you're a 5-person startup or a 200-seat firm, we'll build a plan that fits your budget and your risk tolerance.</p>
          <div class="divider" style="margin:1rem 0 0"></div>
        </div>

        <div class="contact-details">
          <div class="contact-item">
            <div class="contact-icon">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <path d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07 19.5 19.5 0 01-6-6 19.79 19.79 0 01-3.07-8.67A2 2 0 014.11 2h3a2 2 0 012 1.72c.127.96.361 1.903.7 2.81a2 2 0 01-.45 2.11L8.09 9.91a16 16 0 006 6l1.27-1.27a2 2 0 012.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0122 16.92z"/>
              </svg>
            </div>
            <div>
              <div class="contact-label">Phone</div>
              <a href={phoneHref} class="contact-value phone-link">{phoneNumber}</a>
            </div>
          </div>

          <div class="contact-item">
            <div class="contact-icon">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
                <polyline points="22,6 12,13 2,6"/>
              </svg>
            </div>
            <div>
              <div class="contact-label">Email</div>
              <a href="mailto:support@fieldstoneit.com" class="contact-value">support@fieldstoneit.com</a>
            </div>
          </div>

          <div class="contact-item">
            <div class="contact-icon">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0118 0z"/>
                <circle cx="12" cy="10" r="3"/>
              </svg>
            </div>
            <div>
              <div class="contact-label">Address</div>
              <span class="contact-value">{address}</span>
            </div>
          </div>

          <div class="contact-item">
            <div class="contact-icon">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <circle cx="12" cy="12" r="10"/>
                <polyline points="12 6 12 12 16 14"/>
              </svg>
            </div>
            <div>
              <div class="contact-label">Support Hours</div>
              <span class="contact-value">Mon–Fri 7am–6pm / Emergency 24/7</span>
            </div>
          </div>
        </div>

        <a href={phoneHref} class="btn btn-primary contact-phone-btn">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
            <path d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07 19.5 19.5 0 01-6-6 19.79 19.79 0 01-3.07-8.67A2 2 0 014.11 2h3a2 2 0 012 1.72c.127.96.361 1.903.7 2.81a2 2 0 01-.45 2.11L8.09 9.91a16 16 0 006 6l1.27-1.27a2 2 0 012.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0122 16.92z"/>
          </svg>
          Call (503) 555-0142
        </a>
      </div>

      <!-- Right: Map -->
      <div class="contact-map">
        <div class="map-wrapper">
          <div class="map-placeholder">
            <div class="map-overlay">
              <div class="map-pin">
                <svg width="40" height="40" viewBox="0 0 24 24" fill="var(--amber)" stroke="var(--white)" stroke-width="1.5">
                  <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0118 0z"/>
                  <circle cx="12" cy="10" r="3"/>
                </svg>
              </div>
              <div class="map-info">
                <strong>Fieldstone IT</strong>
                <span>812 SW Washington St</span>
                <span>Portland, OR 97205</span>
                <span class="map-note">Serving greater Portland metro since 2012</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .contact {
    background: var(--white);
  }

  .contact-layout {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-3xl);
    align-items: center;
  }

  /* Contact Info */
  .contact-details {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
    margin-bottom: var(--space-xl);
  }

  .contact-item {
    display: flex;
    align-items: flex-start;
    gap: var(--space-md);
  }

  .contact-icon {
    width: 44px;
    height: 44px;
    background: rgba(184, 122, 58, 0.08);
    border-radius: var(--radius-md);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    color: var(--amber);
  }

  .contact-label {
    font-family: var(--font-display);
    font-size: 0.7rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--text-muted);
    margin-bottom: 2px;
  }

  .contact-value {
    font-size: 1rem;
    color: var(--slate);
    font-weight: 600;
    transition: color 0.4s var(--ease-spring);
  }

  .phone-link {
    transition: all 0.4s var(--ease-spring);
  }

  .contact-phone-btn {
    display: inline-flex;
    align-items: center;
    gap: var(--space-sm);
    font-size: 1.05rem;
  }

  /* Map */
  .map-wrapper {
    border-radius: var(--radius-lg);
    overflow: hidden;
    border: 3px solid var(--stone-warm);
    aspect-ratio: 4/3;
    -webkit-mask-image: linear-gradient(to bottom, #000 90%, transparent 100%);
            mask-image: linear-gradient(to bottom, #000 90%, transparent 100%);
  }

  .map-placeholder {
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, var(--slate) 0%, var(--slate-dark) 100%);
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .map-overlay {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-md);
    padding: var(--space-xl);
    color: var(--white);
    text-align: center;
  }

  .map-pin {
    animation: bounce-pin 2s infinite;
  }

  @keyframes bounce-pin {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }

  .map-info {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .map-info strong {
    font-family: var(--font-display);
    font-size: 1.2rem;
    font-weight: 800;
  }

  .map-info span {
    font-size: 0.9rem;
    color: rgba(255,255,255,0.8);
  }

  .map-note {
    margin-top: var(--space-sm);
    font-size: 0.8rem !important;
    color: var(--amber) !important;
    font-weight: 600;
  }

  @media (max-width: 900px) {
    .contact-layout {
      grid-template-columns: 1fr;
    }

    .contact-map {
      order: -1;
    }
  }
</style>